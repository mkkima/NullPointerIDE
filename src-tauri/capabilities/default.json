{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal permissions for the main editor window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "process:default",
    "updater:default"
  ]
}
